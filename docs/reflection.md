# Emotional Damage Reflection

## 1. Ans
* **Part Worked On:** I focused on the File I/O operations (fstream) and string parsing. I implemented the logic to read/write the `users.txt` file.
* **New Thing Learned:** I learned how to serialize objects into strings for file storage and deserialize them back without losing data.
* **If I had one more week:** I would implement encryption for the passwords in the text file so they aren't stored as plain text.

## 2. Usman
* **Part Worked On:** I handled the core STL containers, specifically `std::vector` for Assignments and `std::unordered_map` for user management.
* **New Thing Learned:** I learned the time complexity differences between `map` (O(log n)) and `unordered_map` (O(1)) and why map was better for logins.
* **If I had one more week:** I would add a `priority_queue` for assignments so urgent tasks are automatically processed first.

## 3. Ahmad
* **Part Worked On:** I worked on the OOP structure, designing the `User` base class and the `Teacher`/`Student` derived classes for Polymorphism.
* **New Thing Learned:** I learned how to store derived class pointers (`User*`) in a single container to manage different types of users together.
* **If I had one more week:** I would add an Admin class that has permissions to delete other users.

## 4. Turrab
* **Part Worked On:** I implemented the sorting logic and custom structs in `Models.h`, including the `operator<` overload for Dates.
* **New Thing Learned:** I learned how operator overloading allows standard algorithms like `std::sort()` to work on custom objects like our Dates.
* **If I had one more week:** I would implement a feature to edit the Date of an assignment after it has been created.
